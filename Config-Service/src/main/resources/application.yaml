server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://https://github.com/MorgachevStepan/Bachelor-s-degree-config
          clone-on-start: true
          default-label: main

management:
  endpoints:
    web:
      exposure:
        include: "*"
